{
  "name": "arprov-plugins",
  "version": "1.0.0",
  "description": "SkyStream Arabic plugins repository",
  "scripts": {
    "build": "npx skystream-cli build",
    "validate": "for plugin in */plugin.js; do echo \"Validating ${plugin%/*}...\"; node -c \"$plugin\" 2>&1 || exit 1; done && echo 'All plugins validated!'"
  },
  "devDependencies": {
    "skystream-cli": "latest"
  }
}